Memact LandingPage v2.0

This repository is a static Render deployment.

Render settings:

Build command:
echo "Memact landing ready"

Publish directory:
.

The landing page is intentionally static. It demonstrates Memact as a citation-backed answer engine using sample captured activity and real clickable links.
